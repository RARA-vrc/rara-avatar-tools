# RARA Avatar Tools（アバター軽量化・Quest / iOS対応ツール）

VRChatアバターの **PC軽量化** と **Quest / iOS（Android）対応変換** を、1つの統合ウィンドウ（Avatar Studio）でまとめて行うUnityエディタ拡張です。元アバターは非破壊のまま、PC用『_Opt』とQuest用『_Quest』の複製を生成します。

[![導入ページ](https://img.shields.io/badge/VCC%20%2F%20ALCOM-listing-4c8bf5)](https://rara-vrc.github.io/rara-avatar-tools/) [![License: MIT](https://img.shields.io/badge/License-MIT-green)](./LICENSE)

> Unityメニューに追加される項目は **`RARA / アバター軽量化・Quest・iOS対応ツール`** の1つだけです。このウィンドウ内で対象（PC / Quest）を切り替えて使います。

---

## AAO（Avatar Optimizer）との連携について

本ツールは anatawa12 氏の AAO: Avatar Optimizer と処理を分業する設計です。実行時は、生成した複製にのみ次のAAOコンポーネントを自動で追加・設定します（元アバターには一切追加しません）。

- Trace and Optimize … アバターを走査して自動でできる限りの最適化を行うコンポーネント（未使用ボーンの削減や自動マージなど）。追加の自動最適化のために付与します（下記の Merge Skinned Mesh / Remove Mesh By BlendShape は Trace and Optimize が無くてもビルド時にAAOが適用します）。
- Merge Skinned Mesh … 顔以外の複数の SkinnedMeshRenderer を1つの SkinnedMeshRenderer へ統合します（「SkinnedMesh統合」の実体）。統合時は BlendShape 名を自動変更して重複を避けます。レンダラーをグループ1〜8に振り分けて、まとまりごとに統合するグループ指定モードもあります（顔は常に自動保護）。
- Remove Mesh By BlendShape … 指定した BlendShape で動く頂点とポリゴンを削除します（服の下に隠れる肌などを消す「隠れメッシュ削減」の実体）。

PhysBone のマージ（設定が一致する兄弟チェーンを1本へまとめてコンポーネント数を減らす処理）は本ツール独自の実装で、AAO の Merge PhysBone は使用していません。

適用タイミング: これらの統合・削除は、アップロード/Play時のビルド（NDMF）で実行されます。そのため、生成直後の複製やエディタ上の診断値には反映されず、実際のアップロード後にさらに軽くなります。最終的な数値は VRChat SDK のビルド結果、または本ツールの実測レポート（後述）で確認してください。

AAO未導入時の挙動: AAO が無くても本ツール自体は動作します。AAO を使う機能（SkinnedMesh統合・隠れメッシュ削減など）は自動でスキップされ、ウィンドウ内に導入の案内が表示されます。導入は次のリポジトリを VCC / ALCOM に追加して行います。

- 導入リポジトリ: <https://vpm.anatawa12.com/add-repo>
- 公式ドキュメント: <https://vpm.anatawa12.com/avatar-optimizer/ja/>

謝辞: AAO: Avatar Optimizer（MIT License / anatawa12 氏）に感謝します。あわせて VRChat SDK・lilToon・NonToon・Poiyomi・Modular Avatar / NDMF にも感謝します。

---

## オープンベータ（Open β）・不具合報告

本ツールはオープンベータ(Open β)として公開中です。不具合報告・要望は X(Twitter) の DM (https://x.com/RR_vrchat) または メール (raravrchat@gmail.com) へお願いします。

---

## 1. これは何

VRChatアバターの重さ（パフォーマンスランク）を下げ、必要ならQuest / iOS版へ変換するためのツールです。対象ユーザーを次の3層に分けて設計しています。

- **とりあえずQuest / スマホ対応にしたい人** … シェーダーをMobile系へ変換し、質感をテクスチャへベイクして、Android / iOSで表示できる『_Quest』複製を作ります。
- **PCのまま Very Poor を避けたい人** … シェーダー変換はせず、テクスチャメモリ・マテリアルスロット・メッシュ・PhysBoneを削減して PC（Windows）基準でランクを Poor 以上へ引き上げます。
- **PCで Good / Excellent を狙いたい人** … 同じくPC基準で、より上のランクへ寄せます。

すべての操作は1つのウィンドウで完結します。内部的には **PC最適化エンジン（PC Optimizer）** と **Quest / iOS変換エンジン（Quest Converter）** を共有しています。

### 対応シェーダー

変換元（PC 側）のマテリアルが下記のシェーダーファミリーなら、質感を読み取って Quest / iOS 対応の Toon Standard / Toon Lit へ自動変換します。**lilToon が最も忠実**で、それ以外は「プロパティ読み取り+メインテクスチャへのティント焼き込み」による近似変換です（色調補正は結果色を焼き込んで近づけるため PC 版と完全一致はしません）。**1.3.0** で Unity Standard/Unlit・VRM MToon（VRoid）を変換元として追加し、Poiyomi Toon はロック済みマテリアルまで対応しました。

| シェーダー系統 | Quest / iOS変換 | 引き継ぎ・近似の要点 |
|---|---|---|
| **lilToon** | ◎ 最も忠実 | 色調補正・影・2nd/3rdレイヤー・エミッション・マットキャップ・カリングをベイクして Toon Standard / Toon Lit へ変換。ベイクにlilToon付属のベイク用シェーダーを使用。**マットキャップは Toon Standard の内蔵マットキャップへ引き継ぎ**（下記「マットキャップ」参照） |
| **NonToon**（jp.lilxyzw） | ○ 近似 | ベーステクスチャ・ノーマル・カリングを引き継ぎ、影グラデーションを影ランプへ生成、マットキャップは近似（乗算/加算の2枠→1枠）。エミッションは非搭載のため付かない |
| **Poiyomi Toon**（ロック済み含む） | ○ 近似 | メイン色（色相/彩度/明度/ガンマ）をティント焼き込み、ノーマル・エミッション・カリングを引き継いで Toon Standard へ。影ランプは SDK 既定。**ロック済み（最適化済み）マテリアルも変換可** |
| **Unity Standard / Unlit** | ○ 近似 | メイン色・ノーマル・エミッション・カットアウト/透過方針・カリングを引き継いで Toon Standard / Toon Lit へ。**メタリック/スムースネスは無視**（金属パーツは MatCap Lit 指定を推奨） |
| **VRM MToon**（VRoid） | ○ 近似 | メイン色・ノーマル・エミッション・カットアウト/透過方針・カリングを引き継いで Toon Standard へ。**SphereAdd（スフィアマップ加算）は内蔵マットキャップへ加算で引き継ぎ** |

- 共通で、カットアウト/透過の扱いは従来の透過ポリシー（半透明は乗算/加算で近似・**顔/大型メッシュ/髪は保護して不透明化**・カットアウトは塗りつぶし）に準拠します。**1.4.2 から顔を自動保護** し、顔（アバターディスクリプタのビセーム/まばたきメッシュ、または名前に face・顔 等を含むメッシュ/マテリアル）の基底マテリアルは、透過設定でも半透明の自動再現（パーティクル化）や非表示の対象にせず、常に不透明（Toon Standard）へ変換します（顔の白飛び・消失を防止）。チーク・涙・ハイライトなどのオプション重ねマテリアル（名前で判定）は従来どおり乗算/加算で近似します（既存の大型メッシュ・髪の保護と同じ扱い）。
- **近似・非対応（変換は止めず変換レポートに警告）**: シェーダー固有の色調補正・デカール・ラメ（グリッター）などの特殊表現は再現されません。Unity Standard のメタリックは無視されます（金属は MatCap Lit を推奨）。アウトラインは従来どおり外れます。
- **FakeShadow（前髪影）・OutlineOnly などの効果専用シェーダー** は、Questでは白い不透明面としてベイクされてしまうため **自動で非表示化** されます（ベース面はそのまま変換）。
- PC軽量化のみを行う場合、PCではシェーダーを変換しないため、元マテリアルのシェーダーがそのまま使われます（lilToonは必須ではありません）。

### 動作環境

- **Unity 2022.3系**（VRChat公式指定バージョン）
- **VRChat Avatars SDK `>=3.8.1`**（`com.vrchat.avatars`。変換先の VRChat/Mobile/Toon Standard シェーダーがこのバージョンで追加されたため）
- **レンダーパイプラインは Built-in のみ対応**
- **AvatarOptimizer（AAO）を強く推奨**
  メッシュ・マテリアルスロットの統合や隠れメッシュ削減は **ビルド時にAAOが担当する分業設計** です。AAOが無いと削減効果が出にくく、特にQuestではスキンメッシュ数の上限に収まらず Very Poor になりやすく、既定で相手にフォールバック表示され揺れ物も停止し得ます（アップロード可否自体はサイズ上限のみで決まります。実質必須）。
  導入用リポジトリ: <https://vpm.anatawa12.com/add-repo>
- **Modular Avatar / NDMF は任意**（導入されていれば MergeAnimator・Material Setter・メニュー切替なども変換対象として扱います）

---

## 2. インストール（VCC / ALCOM）

VPMパッケージ `jp.rara.avatar-tools` として導入します。

### ワンクリックで追加

導入ページを開いて「VCC / ALCOM に追加」ボタンを押すか、次のリンクをブラウザで開きます（VCC / ALCOM が起動し、リポジトリ追加の確認画面が開きます）。

- 導入ページ: <https://rara-vrc.github.io/rara-avatar-tools/>
- ワンクリック: `vcc://vpm/addRepo?url=https%3A%2F%2Frara-vrc.github.io%2Frara-avatar-tools%2Findex.json`

### 手動でリポジトリを追加

VCC / ALCOM の **Settings > Packages > Add Repository** に、次のURLを貼り付けます。

```
https://rara-vrc.github.io/rara-avatar-tools/index.json
```

### プロジェクトへの追加

1. 対象のアバタープロジェクトを VCC / ALCOM で開きます。
2. **Manage Project** の一覧から **RARA Avatar Tools** を追加します。
3. Unityへ戻ると、コンパイル後にメニューバーへ `RARA / アバター軽量化・Quest・iOS対応ツール` が追加されます。

追加の設定は不要です。アンインストールは Manage Project からパッケージを外すだけです。

### 旧 unitypackage版からの自動移行

以前 `Assets/RARA/` 以下へ unitypackage で導入していた場合、VPMパッケージが持つ **legacyFolders** の定義により、パッケージ導入時に旧フォルダ（`Assets/RARA/QuestConverter/Editor`・`Shaders`、`Assets/RARA/PCOptimizer/Editor`、`Assets/RARA/AvatarStudio/Editor`）が自動で除去され、`Packages/jp.rara.avatar-tools/` 側へ一本化されます。旧版と新版のスクリプトが二重に存在してコンパイルが衝突することはありません。

---

## 3. クイックスタート（3手順）

1. **プリセットを押す** — シーンのアバター（`VRCAvatarDescriptor`）を選び、目的に近いプリセット（Questだけなら「雑にQuest対応」、両方なら「フル両対応（おすすめ）」）を押します。対象と目標ランクがまとめて設定されます。
2. **⑦ 実行** — 「実行（選択した対象へ生成）」を押して複製を生成します。元アバターは変更されません。
3. **Build & Test** — 生成された複製を VRChat SDK コントロールパネルでアップロード（またはBuild & Test）します。最終的なランク・見た目はSDKで確認します。

---

## 4. ウィンドウ完全ガイド

ウィンドウは上から順に進めば完了する構成です。ヘッダー → アバター選択 → 要約行 → 対象チップ → プリセット → ①〜⑧ステップ、の順で並びます。

### ヘッダー / アバター選択 / 要約行

- **アバター**: シーン上の `VRCAvatarDescriptor` を指定します。「シーンから検出」で自動選択もできます（`_Opt` / `_Quest` 複製は避けて元アバターを優先）。
- **要約行**: 選択アバター名、PC総合ランク、Quest総合ランク、Quest推定ダウンロードサイズを常時表示します（診断キャッシュの読み取りのみで再計測しません）。

### 対象チップ（PC / Quest・既定で両方）

`PC` と `Quest / iOS` のチップで対象を切り替えます。**既定は両方**です。対象を変えると診断はリセットされ、次に診断を開いたとき再計測されます。どちらも未選択だと警告が出て実行できません。

### プリセット（4種）

| プリセット | 対象 | 目標ランク | 補足 |
|---|---|---|---|
| **雑にQuest対応** | Questのみ | Poor | 透過は乗算/加算で自動再現（ストッキング等の透けを保持）。ポリゴン削減は既定オフ |
| **PCをPoorに** | PCのみ | Poor | |
| **PCをGoodに** | PCのみ | Good | |
| **フル両対応（おすすめ）** | PC + Quest | PC=Good / Quest=Poor | 透過は自動再現・ポリゴン削減は既定オフ |

押した後も各ステップで個別に微調整できます。

---

### ① 診断（PC / Quest 現在値と目標）

現在値をPC基準とQuest基準の **2列で並記** します。

- **目標ランクピッカー**: PC・Questそれぞれに `Excellent / Good / Medium / Poor` のトグルがあり、超過項目を強調表示します。
- **総合ランク要約**: PC総合・Quest総合を色付きで表示。ダウンロードサイズが10MB上限を超える見込みのときだけ「Androidアップロード不可（サイズ上限超過）」を明示し、Very Poor は既定でフォールバック表示になる旨を注意表示します。
- **比較テーブル**: 項目ごとにPC現在値・PCランク・Quest現在値・Questランクを並べ、目標を超えた項目を赤字＋⚠で示します。
- **Quest詳細**: 推定ダウンロードサイズ（10MB上限との比較）、Quest非対応シェーダーのマテリアル一覧、Android非対応コンポーネント一覧、テクスチャ設定の警告を表示します。
- **目標達成ガイド**: 目標を超えた項目ごとに推奨アクションと「→ ステップnで対応」ボタンが並び、押すと対応ステップ（テクスチャ・ポリゴン・PhysBone・構成整理・マテリアル等）が開きます。

<details>
<summary><b>PC（Windows）ランク閾値（SDK判定アセットの実値）</b></summary>

各数値はそのランクの上限。値がその数値以下なら少なくともそのランク以内に収まります。Poor を超えると Very Poor です。

| 項目 | Excellent | Good | Medium | Poor |
|---|---|---|---|---|
| 三角数（ポリゴン） | 32,000 | 70,000 | 70,000 | 70,000 |
| スキンメッシュ数 | 1 | 2 | 8 | 16 |
| メッシュレンダラー数 | 4 | 8 | 16 | 24 |
| マテリアルスロット数 | 4 | 8 | 16 | 32 |
| テクスチャメモリ（MB） | 40 | 75 | 110 | 150 |
| ボーン数 | 75 | 150 | 256 | 400 |
| PhysBoneコンポーネント | 4 | 8 | 16 | 32 |
| PhysBone対象Transform | 16 | 64 | 128 | 256 |
| PhysBoneコライダー | 4 | 8 | 16 | 32 |
| PhysBone衝突判定回数 | 32 | 128 | 256 | 512 |
| コンタクト（ローカル除く） | 8 | 16 | 24 | 32 |
| コンストレイント | 100 | 250 | 300 | 350 |

ポリゴンは Excellent のみ 32,000 で、Good・Medium・Poor はすべて 70,000。32,001〜70,000 は Good に載り、70,000 を超えると一気に Very Poor になります。PC の Very Poor はアップロード自体は可能です（相手の設定で非表示になり得る）。
</details>

<details>
<summary><b>Quest / iOS（Android）ランク閾値</b></summary>

iOS は Quest（Android）と同じモバイル制限が適用されます。

| 項目 | Excellent | Good | Medium | Poor |
|---|---|---|---|---|
| 三角数（ポリゴン） | 7,500 | 10,000 | 15,000 | 20,000 |
| スキンメッシュ数 | 1 | 1 | 2 | 2 |
| マテリアルスロット数 | 1 | 1 | 2 | 4 |
| PhysBoneコンポーネント | 0 | 4 | 6 | 8 |
| PhysBone対象Transform | 0 | 16 | 32 | 64 |
| PhysBoneコライダー | 0 | 4 | 8 | 16 |
| コンタクト | 2 | 4 | 8 | 16 |
| テクスチャメモリ（MB） | 10 | 18 | 25 | 40 |

- アップロード可否はサイズのみで決まる: ダウンロードサイズ ビルド後圧縮 10MB / 展開後 40MB を超えると SDK がアップロードをブロックする（ランクでは決まらない）。
- 表示: モバイルの既定表示ランクは Medium。Very Poor は既定でフォールバック表示になるが、相手は Show Avatar で表示可能（公式は将来この表示制限の強化を予告）。
- **PhysBoneコンポーネントが8個（Poor上限）を超えると、モバイルでは PhysBone・コンタクト・コンストレイントがすべて無効化** され、揺れものが全て停止します。
</details>

---

### ② 構成整理（トグル / SkinnedMesh統合）

トグルの固定とSkinnedMeshの統合を設定します。どちらも **統合の実体はビルド時にAAOが実施** するため、表示される数値は概算です。

**衣装・トグル整理**（トグル固定の2経路）
- 各グループを `維持 / 常時表示 / 非表示除去` から選びます。
  - **常時表示** = 常時ON固定にしてトグルアニメを外す → AAOがビルド時に同一メッシュへ結合でき、SkinnedMesh数・スロット数が減る。
  - **非表示除去** = そのメッシュをEditorOnly化して除去（AAO不要）。
- 一括ボタン「**現在の表示状態で固定（全トグル）**」「**すべて維持に戻す**」、各グループの「ピン」でシーン上をハイライトできます。固定するトグル数（表示固定/非表示固定の内訳）を予測表示します。
- 各グループに **「非表示固定で減る専有アセット」の削減量チップ（圧縮後 -◯MB / 展開後 -◯MB）** を表示します。専有アセットとは、そのグループのサブツリー内のレンダラーだけが使い、外では一切使われないテクスチャ・メッシュです。素体などと共有するアセットは非表示にしても減らないため計上せず、共有アセットしか持たないグループは「**共有テクスチャのみ**」（単独では減らない）と表示します。一覧は専有削減量（展開後）の大きい順に並べ替えられます。
- サイズが上限を超えているときは、削除候補を専有削減量の大きい順に提示するガイドを表示します（「上位◯件でカバーできる見込み」の合計は、親子（入れ子）の衣装グループを二重計上しません）。

**SkinnedMesh統合（顔以外を1つへ / グループ指定）**
- `統合しない / 顔以外を統合（推奨） / グループ指定で統合` を選びます。顔（ビセーム / まばたきメッシュ）以外の SkinnedMeshRenderer を1つへ統合し、SkinnedMesh数・スロット数を削減します（Questのスキンメッシュ上限2対策）。
- グループ指定モードでは、顔以外を一律に1つへまとめるのではなく、レンダラーごとにグループ1〜8を割り当て、同じグループのレンダラーだけを1つのメッシュへ統合します。素体と衣装を別メッシュに保ちたいときなどに使います。顔メッシュは常に自動保護されてグループへ割り当てできず、どのグループにも割り当てなかったレンダラーは統合されません。グループごとの「n枚 → 1枚」と統合後の内訳（グループ統合 + 顔（自動保護） + 統合しない = 統合後の概算枚数）を表示します。
- 顔メッシュ・Clothの付いたメッシュ・EditorOnly・プレビューで「統合しない」を選んだメッシュは対象外です。統合前→統合後（概算）のSMR数と、レンダラーごとの「統合する/しない＋理由」を表示します。
- **注意（トグル無効化）**: 統合先は常時表示になるため、統合したメッシュのON/OFFトグルは効かなくなります（切り替えを残したいメッシュは「統合しない」を選ぶか、先に「常時表示」で固定してから統合します）。表情・ブレンドシェイプアニメは、AAOがブレンドシェイプ改名・スロット再マップ・アニメ再パスを自動で行うため追従します。
- **NDMF コンソールの AAO 警告について**: 変換直後の即時実測（1.4.0 以降）や ▶️（Play）・ビルド時に、NDMF コンソールへ AAO 発の警告（統合ソース間の Root Bone / Anchor Override の食い違い、同名 BlendShape の値不一致）が出ることがあります。これらは **1.4.0 以前からビルド時に発生していたもの** で、即時実測で毎回コンソールを通るようになって見えやすくなっただけで、新しい不具合ではありません。**1.4.1** では統合先へ Root Bone と Anchor Override を自動設定して食い違いを抑え、アニメで使われていない同名 BlendShape の値衝突は **現在値でメッシュへ焼き込んで自動固定**（見た目は変わりません）します。アニメで動かしている BlendShape は表情などが壊れないよう自動固定せず、**変換レポートで案内** します。なお AAO が出す最適化結果のログ（「!」アイコン付きで削減量などを報告するもの）は **エラーではなく正常な成果報告** です（赤字のエラーでなければ変換は成功しています）。

**Quest固有（対象がQuestのとき）**
- **Quest除外（ビルド除外パス）**: ドラッグ&ドロップで登録したオブジェクトを、Quest複製でのみ EditorOnly化してビルドから完全除去します（PC版には影響しません）。解決チェック・ピン・削除つき。アバタールートやスキンメッシュが参照するボーンは登録できません（同名兄弟・名前に「/」を含むものも検証で弾かれます）。
  - **1.5.1**: EditorOnly タグ付き・Quest除外に登録したオブジェクトは、診断だけでなく各セクションの一覧・プレビュー（マテリアル・衣装/トグル整理・SkinnedMesh統合・PhysBone・ポリゴン削減・隠面メッシュ・テクスチャ）と変換からも自動で外れます（ビルドに含まれないため。統合前後の概算にも数えません）。非表示にした項目があるセクションには「EditorOnly/Quest除外のため n 件を非表示（ビルドに含まれないため）」を1行表示します。この登録パネルだけは全件を表示し、既に EditorOnly タグが付いているものには「EditorOnly」バッジを付けます。除外配下だけで使われるマテリアルは変換もスキップし、レポートに件数を出します（混在利用は従来どおり変換）。
- **隠面メッシュ削減**: 服の下に隠れて見えない肌などを、アバターが持つ shrink（縮小）ブレンドシェイプで削除します（AAO必須。未導入時は導入CTAを表示）。候補ごとにチェック・使用ブレンドシェイプ名・理由・ピンを表示。shrinkブレンドシェイプが無い場合は AAO の Remove Mesh in Box / By Mask / By UV Tile による手動手引きを表示します。
  - 検出の仕組み: ① ブレンドシェイプ名が隠し/縮小系トークン（shrink / hide / 縮小 / 非表示 / 削除 / 消し / 隠し 等）を含むもの、または部位トークン（肌 / 素体 / パンツ / インナー 等）とオフ系トークン（off / オフ / なし 等）が同居するものを候補化。② 顔まわり（顔 / 目 / 眉 / 口 / 涙 / まつ毛 / ほくろ、あ・い・う・え・お、まばたき 等）は名前・ジオメトリの両面で除外し、表情・MMD標準モーフ・リップシンクの誤削除を防ぎます。③ 名前が一致しなくても、最終フレームで頂点の大部分（全体の約40%以上）を強く動かす形状だけを幾何判定で候補化します（保守的な閾値・頂点数が少なすぎるメッシュは対象外）。
  - 穴に注意: 削除されるのは選んだシェイプが動かす領域だけ（＝制作者が「隠れる前提」で設計した箇所）です。そのため、その服を後から非表示にすると下の肌が無くなって穴が開いて見えます。削除は変換後の複製の Remove Mesh By BlendShape がビルド時に適用するため、そのコンポーネントのプレビューで削除後の状態を確認できます（元アバターは無改変）。

---

### ③ PhysBone 整理

残す・削除・マージを設定してコンポーネント数を削減します（揺れは維持）。

- **マージ設定**: `設定一致チェーンをマージ` と `設定違いも先頭へ統一（ルーズ）` の2種。ルーズは設定が完全一致しない兄弟チェーンも先頭の設定に統一します（アニメ制御・パラメータ使用などの安全ゲートは維持）。
- **選択後コンポーネント数** を、対象（PC / Quest）ごとの上限メーター・**ランク別コンポーネント上限ラダー**・次ランクまでの削減目安と併せて色付き表示します。
  - PC上限ラダー: **Excellent 4 / Good 8 / Medium 16 / Poor 32**
  - Quest上限ラダー: **Excellent 0 / Good 4 / Medium 6 / Poor 8**
- **優先度で自動選択（Quest 8本まで）**: 髪・胸などの名前パターンで優先度を判定し、Quest上限（8本）内に収まるよう自動でPhysBoneを残し、残りを「削除」に設定します。**Questで8本を超えるとモバイルで全ての揺れが停止するため**、超過時は赤の強警告を表示します。
- **行の操作**: グループ行は「マージ n本 → 1本」表示＋「**マージしない**」（このグループを1本へまとめず個別に残す）＋「削除」。単独行は「削除」。削除したPhysBoneは別枠の「削除指定」一覧に移り、「**戻す**」で復元できます（見つからないパスは黄色で表示）。
- PCは上限を超えてもアップロード可能（ランクが下がるだけ）です。

---

### ④ テクスチャ縮小

縮小しても **元テクスチャとそのインポート設定は一切変更せず**、生成時に低解像度コピーを作って複製がそれを参照します。

**PC（VRAM）**
- 現在のPCテクスチャメモリを目標ランク上限と比較表示。寄与の大きいものから縮小提案が並びます。
- 「**全て縮小計画へ追加**」「**目標まで自動調整**」（目標MBを下回るまで効果の大きい順に登録）「**縮小計画をクリア**」、各行の「追加」で個別登録できます。

**Quest（ダウンロード / 展開後サイズ）**
- 推定サイズを、独立した2つの上限 **圧縮後（ダウンロード）10MB** と **展開後（非圧縮）40MB** に並べて判定します（テクスチャ / メッシュ / アニメの内訳つき）。どちらか一方でも超えるとSDKがアップロードをブロックするため、超過している側を色付きで示します。
- 「**上限内まで自動調整（圧縮後10MB・展開後40MB）**」（確認ダイアログで内容を見てから一括登録）「**縮小計画をクリア**」。ダウンロードサイズの大きいテクスチャ上位を一覧し、Android向けインポート上書きが無いものには「Android上書きなし」バッジを付けます。縮小・削減提案から個別に登録もできます。
- テクスチャを最小（縮小フロア）まで下げても届かない場合（展開後がメッシュ・ブレンドシェイプ支配のときなど）は、その旨を正直に表示し、テクスチャ縮小だけでは収まらないこと（⑤ ポリゴン削減・Quest除外・不要ブレンドシェイプ整理の併用）を案内します。

---

### ⑤ ポリゴン削減（Quest）

Quest対象のときに設定できます。**既定は無効**（明示的に有効化）。

- 「**ポリゴン削減を有効化**」トグル。無効のまま現在の三角形数が目標を超えていると、琥珀の案内と「**ポリゴン削減を有効にする**」ボタンが出ます。
- **目標ランク**を `Excellent 7,500 / Good 10,000 / Medium 15,000 / Poor 20,000` から選ぶか、目標三角形数を直接入力します。「**自動で配分計画を作成**」でメッシュごとに削減目標を配分します（優先度の低い その他 → 衣装 → 素体 → 髪 → 顔 の順に削る）。「計画クリア」あり。
- 各行は **カテゴリバッジ（顔 / 髪 / 素体 / 衣装 / その他）** ＋現在→目標（編集可能）＋「戻す」（自動配分値へ復帰）＋ピン。
- 頂点を間引く方式で **UV・法線・ボーンウェイト・ブレンドシェイプ（表情）を保持** します。**顔・髪は品質下限で強く保護**。Unity Cloth の付いたメッシュは自動で対象外です。大きく削る場合は先に外部ツール（Blender等）で整えるのが推奨です。

---

### Meshia との併用（推奨）

**Meshia Mesh Simplification**（Ram.Type-0 氏 / MIT）は、NDMF で **ビルド時（Play / アップロード）** にメッシュを高品質に簡略化するツールです。レンダラー単位の `Meshia Mesh Simplifier`、または（Modular Avatar 導入時）アバター単位の `Meshia Cascading Avatar Mesh Simplifier` で、目標三角形数などをレンダラーごとに細かく指定できます。導入は VPM リポジトリ <https://ramtype0.github.io/VpmRepository/> を VCC / ALCOM に追加して行います（公式ドキュメント <https://ramtype0.github.io/Meshia.MeshSimplification/>）。

**役割分担・併用パターン**

- **品質重視（Meshia を主に）**: 崩れやすい／見せ場のモデルは Meshia にレンダラー単位で任せ、EditMode プレビューで品質を追い込みます。本ツールのポリゴン削減は **オフのまま**（または Meshia を付けたレンダラーは本ツールの対象から外す）にして二重削減を避けます。
- **手早く予算内（本ツールを主に）**: 目標ランクへ収めたいだけなら、本ツールのポリゴン削減だけを使います（目標三角形数へ自動配分・顔／髪を保護・`_Quest` 複製へ即時適用）。Meshia は付けません。ワンショットで完結し、二重削減の心配がない既定運用です。
- **役割分担（併用）**: 顔・髪など品質が要る部位だけ Meshia で丁寧に指定し、残りは本ツールの予算配分で目標まで詰めます。**同一レンダラーに両方を効かせない**（レンダラー単位でどちらか一方へ割り当てる）のが最も安全です。

**注意（ビルド時反映・二重削減）**: Meshia の削減は **ビルド時** に反映されるため、**エディタ／診断のポリゴン数には出ません**（EditMode プレビューはありますが、実メッシュの差し替えはビルド時です）。本ツールと Meshia の両方を同じ目標へ強くかけると、ビルド後に目標を大きく下回って **削りすぎ** になります。併用時は、どちらか一方に予算（目標三角形数）を持たせ、本ツールの目標は Meshia 適用後に残る想定数を差し引いて設定してください。⑤ ポリゴン削減パネルは Meshia コンポーネントを検出すると併用注意を表示します。

**謝辞**: Meshia Mesh Simplification（MIT License / Ram.Type-0 氏）に感謝します。

---

### ⑥ マテリアル（PC / Quest）

PC列（アトラス統合）とQuest列（変換方法）を並べて設定します。

**PC（アトラス統合）**
- 「PCマテリアルをアトラス統合」トグル。互換マテリアルを1枚のアトラスへまとめてスロット数・テクスチャメモリを削減します。
- オプション: アトラス最大サイズ（1024 / 2048）、**テクスチャ無し（色だけ）の材質も統合**（色をセルへ焼き込む）、**エミッションをアトラスへ焼き込む**、**カリング差を無視**（統合後は両面描画）、**アウトライン**（`統合しない / 外して統合（推奨） / 付きに統一`）。
- 統合予定グループ（両面化・アウトライン付与/除去の注記つき）と、統合できない/単独マテリアルを **理由つき** で表示。マテリアルごとにアトラス除外も指定できます。

**Quest / iOS（マテリアル変換）**
- **変換先シェーダー**: `Toon Standard`（影ランプ等対応） / `Toon Lit`（最軽量）。
- **半透明の既定処理（透過3モード）**: `自動で半透明を再現（Emulate・推奨） / 非表示にする（Hide） / 不透明に変換（Opaque）`。
- 各マテリアルの **個別変換方法（8種）**: `自動 / Toon Standard / Toon Lit / MatCap Lit（金属・ツヤ向け） / 加算（半透明） / 乗算（半透明） / 非表示 / 変換しない`。行ごとに「変換後にどうなるか」を表示。
- **状態バッジ**: 透過 / カットアウト / パーティクル / アニメ使用 / メニュー・ギミック参照 / TMP / 破損 / 対応済。
- Quest版で非表示になる予定のマテリアルがあると件数を案内し、その **非表示行にクイックボタン「不透明にする」「乗算で再現」** を出します（ストッキング等を見えるように戻せます）。

**マットキャップ（金属・ツヤの映り込み）**

金属パーツやツヤのある面に使う「マットキャップ（MatCap）」を、Quest / iOS でも近い見た目で再現します。

- **Toon Standard への自動引き継ぎ（既定）**: lilToon のマットキャップは **1.1.0** から、変換先の Toon Standard が持つ内蔵マットキャップへ自動で引き継がれます（設定不要）。NonToon も従来どおり近似変換され（乗算 / 加算の2枠を Toon Standard の1枠へ近似）、VRM MToon の加算スフィア（SphereAdd）も加算マットキャップとして引き継がれます。**Poiyomi のマットキャップは自動では引き継がれません**（変換レポートに表示されます）。金属パーツなど映り込みを残したい場合は、マテリアル別指定で「MatCap Lit（金属向け）」を選ぶと Poiyomi のマットキャップ画像を引き継いで変換できます。
  - **引き継がれるもの**: マットキャップ画像 / 合成モード（**乗算はそのまま乗算**、通常・加算・スクリーンは**加算で近似**）/ 強さ / マスク。
  - **引き継がれないもの**: 2ndマットキャップ（2枚目）、カスタム法線・視差（パララックス）・カスタム UV などの高度なオプション（変換レポートに警告）。
  - **オフにする**: Quest 変換設定の詳細設定でマットキャップの引き継ぎをオフにできます。
- **MatCap Lit（金属パーツ向けの変換先）**: 変換方法で変換先に `VRChat/Mobile/MatCap Lit` を選べます。**メインテクスチャ × マットキャップの乗算にライティングを足したシンプルなシェーダー**で、金属のツルッとした映り込みに最適です。
  - **制約**: 不透明のみ / ノーマルマップ・影ランプなし / マットキャップの性質上 **アトラス統合の対象外**（マテリアルスロットを1つ使う）。
  - **使い分けの目安**: 通常は自動引き継ぎ（Toon Standard）で十分です。**より強い金属感が欲しいパーツだけ MatCap Lit** を選びます。
- **アトラス統合との関係**: 異なるマットキャップを持つマテリアル同士は、同じアトラスに統合されません（見た目を守るための仕様）。マットキャップを使うマテリアルはスロットを1つ使うことを見込んでおいてください。

**Quest固有：アトラス統合（実験的）**
- 「アトラス統合を有効化」＋アトラス最大サイズ（1024 / 2048 / 4096）＋「**影ランプを統一してグループ化**」（影ランプの違いを無視してスロット削減優先。影ランプが個別生成されるアバターでオフだと統合がほぼ効かない）。マテリアルごとに統合対象へ含める/外すを指定でき、**統合できない理由の見方**（アニメ差し替え・UVタイリング等）も対象外行に表示します。

**Quest固有：表情デカール（チーク / 涙 / アイハイライト）**
- 既定処理が Emulate のときは乗算/加算で再現され非表示化されない旨を案内。Hide / Opaque のときは非表示化される旨と、検出されたデカール一覧（ピンつき）を表示します。

**Quest / iOS 変換設定（実行前の最終確認・末尾）**
- 最大テクスチャサイズ（512 / 1024推奨 / 2048）、Android圧縮形式（ASTC 4x4 / 5x5 / 6x6推奨 / 8x8）。
- シェーダー別: **影ランプを生成**（Toon Standard）、**エミッションを変換**、**影をメインテクスチャへベイク**（Toon Lit）、**リムライトを近似変換**（Toon Standard・既定オフ）。
- **単色・低ディテールなテクスチャを極限まで縮小**（既定オン）。
- 詳細設定: アニメーションも変換 / ⚠非対応コンポーネントを削除 / UnityコンストレイントをVRCConstraintへ変換 / ⚠マージ後もPoor上限超過分を削除 / 出力先フォルダ（既定に戻す）/ **Android・iOSビルド時に非対応コンポーネントを自動削除（Auto Fix相当）**。

---

### ⑦ 実行

- **取り残された複製の再確認**: 元アバターをリネームした後に残った古い『_Opt』『_Quest』複製があると名前付きで警告します（自動削除はしません）。
- **実行オプション**: 「生成した複製をプレファブとして保存する」「AAO（Trace and Optimize）を複製へ付与する」。
- **この実行で行われること**: 対象・目標ランク・透過処理・ポリゴン削減の有無などのプリフライト要約を箇条書き表示。Quest対象なのにビルドターゲットがAndroidでない場合は注意を出します。
- **実行（選択した対象へ生成）**: 確認ダイアログの後、選択対象へ複製を生成します。元アバターは無改変。
- **実行結果**: パイプラインごとのレポート（コピー可）、**変換前後の比較テーブル**（項目 / 変換前 / 変換後 / 判定。PC・Quest列で目標と突き合わせ）、**変換結果チェック**（元 ← → 変換後マテリアルのサムネイル比較）。
- **アップロード手順ガイド（4手順）** が常設されます。
  1. ビルドターゲットを **Windows** に切替 → **_Optだけをアクティブ化** → SDKパネルで **_Opt** をアップロード。
  2. PC版の **Blueprint ID を Quest版へ同期**（別IDがある場合は確認ダイアログ）。
  3. ビルドターゲットを **Android** に切替。
  4. **_Questだけをアクティブ化** → SDKパネルで **_Quest** をアップロード。PC版と同じBlueprint IDのまま上げると、1つのアバターのPC / Quest両対応（フォールバック）になります。
  現在のビルドターゲットと各複製のBlueprint IDを状態表示し、_Optと_Questが同一IDかどうかも色付きで示します。
  - **_Optだけ / _Questだけをアクティブ化（1.5.1）**: 手順①・④のボタンで、アップロードする複製だけをシーンでアクティブにし、元アバターともう一方の複製（_Opt / _Quest）だけを非アクティブにします（無関係なアバターや非アバターには触れません。Undoで元に戻せます）。SDKパネルはアクティブなアバターを一覧するため、誤って別のアバター（PC用シェーダーのままの _Opt や元アバター）を選んでしまう事故を防げます。ボタンの隣に現在のアクティブ状態（_Opt / _Quest / 元）を表示します。
  - **ビルドターゲットとの食い違い警告（1.5.1）**: ビルドターゲットがAndroid / iOSなのに _Quest が非アクティブで元または _Opt がアクティブなときは、赤い警告（SDKパネルでPC用アバターを選んでいるとMobile非対応シェーダーの警告が大量に出てビルドできない旨）を表示し、手順④の「_Questだけをアクティブ化」を促します。逆にWindowsで _Opt が非アクティブ・_Quest がアクティブなときは軽い情報表示を出します。
  - **⚠ Multi-Platform Build & Publish は使わない（1.5.1）**: SDKの「Multi-Platform Build & Publish」（複数プラットフォーム同時ビルド）は使わないでください。本ツールはPC版とQuest版を別々の複製でアップロードする方式のため、プラットフォームは1つずつ選び、手順①〜④の順に進めます。

**繰り返し再生成の安全性**: 元プレファブは非破壊で、生成物は別の複製です。実行のたびに前回の『_Opt』『_Quest』複製は自動で作り直されるため複製は蓄積しません。生成アセットは決まった場所へGUID保持で上書きされ、参照は壊れません（Quest変換ではアニメーターコントローラーのみ技術的制約で作り直しになります）。

**MA（Modular Avatar）互換**: 変換はビルド前のクローン編集で行うため、MAがビルド時に統合・削除・パス乗っ取りする対象と齟齬が起こり得ます。本ツールはMA参照をすべてリフレクション経由で監査し（未導入時は自動でno-op）、除外/非表示サブツリーへのMA参照、Merge Armature使用時の計測注記、ShapeChangerの二重削除回避、Merge Animator経由クリップの識別、理解外のMAコンポーネント検出などを **自動でガード・警告** します。

---

### ⑧ ヘルプ / はじめての方へ

かんたんな説明（3行）、軽くなる仕組み（処理→何が減るか）、Quest / iOS版の割り切り、迷ったら3ステップ、4段階ランクの主要しきい値の目安をまとめています。

---

### 実測レポート（別ウィンドウ・変換直後の自動実測 / ビルド/Play 時の実測）

診断は「見積り」ですが、実測レポートはビルド/Play 時の最終的な複製そのものを VRChat SDK と同じ計算で実測して確認できる別ウィンドウです。メニューの `RARA / 実測レポート` から開きます。MA（Modular Avatar）・AAO・Meshia・lilToon などの適用後、`EditorOnly` 除去後の本当の数値が分かるため、診断のエディタ数値が控えめに出る分をここで確認できます。**1.4.0** からは、変換が完了すると自動で **ビルド不要の即時実測** も走り、生成直後の複製の最終値を ▶️（Play）や SDK ビルドを待たずにその場で確認できます（下記）。

- **変換直後の自動実測（ビルド不要・1.4.0）**: 変換が完了すると、自動で「ビルド不要の実測」が走ります。NDMF の手動ベイク API で、ビルド時と同じ MA（Modular Avatar）・AAO などの処理を一時コピーへ適用し、数秒で測定します（測定に使った一時コピーと生成した中間アセットは自動削除）。▶️（Play）や SDK ビルドを待たずに、生成直後の複製の最終ランク・項目値をその場で確認できます。ウィンドウの「**今すぐ実測（ビルド不要）**」ボタンからも同じ即時実測を実行でき、自動実測は設定でオフにできます（既定オン）。
- **即時実測の範囲（正直な注記）**: ランク・ポリゴン数などの計算はビルド/Play 時の実測と同一の結果です。ただし **ダウンロードサイズ（10MB 判定）の実ファイルサイズは、本物の SDK ビルド/アップロードでしか得られないため即時実測では推定表示のまま** です。lilToon など SDK ビルド固有の処理も即時実測には含まれません（この点は ▶️（Play）・SDK ビルド時の実測のほうがより厳密）。NDMF 未導入時は即時実測をスキップします。
- ▶️（Play）で実測: `_Opt` / `_Quest` 複製を ▶️（Play）すると、ビルド時と同じ前処理を通した最終複製を実測し、結果がポップアップ表示されます。PC 基準・Quest 基準の両方の総合ランク、項目別の値、目標ランクとの達成判定、元アバターとの差を並べます。
- 実バンドルサイズの記録: SDK の Build & Test / Upload でビルドすると、生成されたアセットバンドルの実ファイルサイズも記録されます（エディタの推定ではなく実際にアップロードされるサイズ。Quest / Android は 10MB 上限との判定つき）。
- 対象と再計測: 既定では `_Opt` / `_Quest` 複製だけを計測します。「_Opt / _Quest 以外のアバターも計測する」で全アバターを対象にでき、編集モードでは「再計測」でスタジオ診断による測り直しもできます。
- オフにする方法: 計測を止めるにはウィンドウ上部の「ビルド/Play時に実測する」をオフにします（ビルドや Play の動作自体には影響しません）。自動表示だけを止めるには「▶️（Play）・ビルドのたびに実測レポートを表示する」をオフにします。

---

## 5. 軽くなる仕組み

| 処理 | 何が減るか | 備考 |
|---|---|---|
| **トグル固定（常時表示）** | SkinnedMesh数 | 表示アニメを常時ONに固定 → AAOがビルド時に同一メッシュへ結合（**要AAO**） |
| **トグル固定（非表示に固定）** | SkinnedMesh数 | 使わない衣装をEditorOnly化して除去（AAO不要） |
| **SkinnedMesh統合** | SkinnedMesh数 | 顔（ビセーム/まばたき）以外を1つへ（結合はビルド時にAAO） |
| **アトラス統合** | マテリアルスロット数 | 複数マテリアルのテクスチャを1枚へ（同一メッシュ内は変換時、メッシュをまたぐ統合はビルド時にAAO） |
| **テクスチャ縮小** | テクスチャメモリ（VRAM）・DLサイズ | 解像度を下げる |
| **ポリゴン削減** | 三角数 | 頂点を間引く（表情のブレンドシェイプ・UVは保持） |
| **PhysBone整理** | PhysBoneコンポーネント数 | 揺れもの設定を選別・マージ |
| **透過処理** | （ランク削減ではない） | Questに半透明シェーダーが無いため、乗算/加算のParticlesシェーダーで見た目を近似（Quest対応のための処理） |

> **ビルド時適用の注意**: AAO（Trace and Optimize）によるメッシュ / スロット統合や隠れメッシュ削減は **ビルド（アップロード）時に反映** されます。そのため生成直後にシーン上・診断表で見える数値は最終的なビルド後より控えめ（改善が少なめ）に出ます。実際のアップロード後はさらに軽くなる場合があります。最終確認はSDKのビルド結果、または実測レポート（`RARA / 実測レポート`。_Opt / _Quest 複製の ▶️ 実行や SDK ビルドで最終複製を実測）で行ってください。

---

## 6. トラブルシューティング

**「見えない / 消えた」系**
- **Quest版で髪・衣装が消えた**: 大型メッシュ・髪は透過自動非表示の対象外です。再変換すれば直ります。透過衣類（ストッキング等）が消えた場合は、⑥マテリアルの該当行で「不透明にする」「乗算で再現」を選びます。
- **メニューで切り替える目・衣装がQuestで消える**: メニュー / ギミック参照のマテリアルも自動変換されます。⑥で「メニュー・ギミック参照」バッジが付いているか確認してください。
- **瞳のテクスチャが消える（PC）**: MAのMaterial Setter等で差し替わる瞳マテリアルは自動でアトラス除外されます。残る場合は手動でアトラス除外に指定します。

**「見える / 出てしまう」系**
- **チーク等が顔に板として乗る**: 既定は「自動で半透明を再現」で乗算/加算に近似されます。板に見える場合は⑥で既定処理や個別指定を確認します。
- **変換後に一部が真っ白に光る（白髪化含む）**: エミッション引き継ぎが原因のことが多く、最新版では自動対処されます。再変換で改善。残る場合は生成マテリアルでEmissionを手動調整します。
- **まぶた等に謎のハイライト**: 「リムライトを近似変換」をオフのままにしてください（既定オフ）。

**サイズ・ランク系**
- **ダウンロードサイズが10MBを超える**: 効果が大きい順に、①全テクスチャを1024px（さらに顔・体以外を512px）、②ASTCブロックサイズを大きく（6x6→8x8で約半分）、③使わないオブジェクトをQuest除外、④AAO Trace and Optimizeを有効化、⑤メッシュ側の削減（ポリゴン削減・不要ブレンドシェイプ削除）。
- **Very Poorになる**: 素体+衣装はスキンメッシュ数が上限を超えやすいのが主因です。SkinnedMesh統合・トグル固定でメッシュ数を減らし、AAOを導入してください。スロットが減らないときは、SkinnedMesh数そのものを減らす（SkinnedMesh統合）とスロットも同時に下がります。

**古い複製 / 変換が効かない系**
- **変換したのに軽くならない / PhysBoneが減っていない**: 前回の古い複製をアップロードしている可能性が高いです。最新版は再変換時に古い複製を自動削除して新しい複製をハイライトするので、ハイライトされた複製をアップロードしてください（SDKパネルの「Previous build of ...」は前回ビルドの数値です）。
- **SDKビルド画面に赤いシェーダーアラート**: 元アバターを選んでいないか確認。アップロードするのは『_Opt』『_Quest』の方です。
- **生成マテリアルのシェーダーを手で変えても戻る**: 生成された複製（『_Opt』『_Quest』）のシェーダーを手動で差し替えても、次の再変換（再生成）で上書きされます。恒久的に変えたい場合は⑥マテリアルの該当行「変換方法」で指定してください。

> いずれの診断値も **エディタ上の状態で計算** されるため、AAO等がビルド時に統合・削減する分は反映されず実際より悪めに出ることがあります。サイズも推定値です。最終確認はSDKのビルド結果で行ってください。

---

## 7. 更新履歴・ライセンス・謝辞

### 最近の更新（v1.2.0 以降）

- **v1.5.1** — EditorOnly / Quest除外のオブジェクトを各一覧（マテリアル・トグル整理・SkinnedMesh統合・PhysBone・隠れメッシュ候補）から非表示に（「n件を非表示」の案内行つき。Quest除外パネルは全表示のまま）。アップロード手順ガイドに「_Opt / _Quest だけをアクティブ化」ボタン・ビルドターゲット不一致の警告・Multi-Platform Build & Publish 非対応の注意を追加
- **v1.5.0** — 削減サイズの見える化: 衣装・トグルグループごとに「非表示固定で約 -X MB（圧縮後/展開後）」を表示。展開後（非圧縮）40MB 上限の推定・判定を追加し、自動調整が圧縮後 10MB・展開後 40MB の両上限対応に
- **v1.4.2** — 顔が真っ白（のっぺらぼう）になる問題を修正。顔のマテリアルを自動保護し、透過設定でも常に不透明で変換（チーク・涙などの重ねは従来どおり乗算/加算で近似）
- **v1.4.1** — SkinnedMesh 統合先への Root Bone / Anchor Override 自動設定と、同名ブレンドシェイプ衝突の自動固定（NDMF コンソールに出ていた AAO 警告を解消）
- **v1.4.0** — ビルド不要の即時実測: 変換直後にビルド時の姿（NDMF 手動ベイク）を自動計測してレポート表示。「今すぐ実測（ビルド不要）」ボタン追加
- **v1.3.0** — 変換元シェーダー拡張: Poiyomi Toon（ロック済み含む）・Unity Standard/Unlit・VRM MToon（VRoid）に対応
- **v1.2.0** — Network ID 自動割り当て: PC/Quest 間で揺れ物を掴んだときの同期ズレを防止

- **更新履歴（全文）**: 同梱の [CHANGELOG.md](./CHANGELOG.md) を参照してください。
- **ライセンス**: [MIT](./LICENSE)。
- **謝辞**: VRChat SDK、AvatarOptimizer（AAO）、Meshia Mesh Simplification（Ram.Type-0 氏 / MIT）、Modular Avatar / NDMF、lilToon、NonToon、Poiyomi。
